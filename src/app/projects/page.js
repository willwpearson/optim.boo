'use client'
import React, { useRef, useState, useCallback } from "react"
import Router from "next/router"

export default function Projects() {
    return (
        <main className="bg-gradient-to-b from-violetDark via-plum to-midnightDark w-screen h-screen flex items-center justify-center">

        </main>
    )
}